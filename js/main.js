'use strict';

(function () {
  var initWizard = function () {
        console.log(window.utils.getEyesColor());
  return  window.utils.getEyesColor;

  };
window.getWizard.getColor(window.utils.getEyesColor);
window.wizardSetting.onClick(window.utils.getEyesColor());

})();
